# Edge Client Requirements - All Options
# 
# This file contains all edge dependencies. For cleaner installs, use specific files:
#
# For CPU-only edge inference:
#   pip install -r requirements-edge-cpu.txt
#
# For CUDA GPU edge inference:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements-edge-gpu.txt
#
# For OpenVINO NPU edge inference:
#   pip install -r requirements-edge-npu.txt
#
# The specific files avoid dependency conflicts and unnecessary packages.

# Core ML dependencies
torch>=2.0.0
torchaudio>=2.0.0
torchvision>=0.15.0
transformers>=4.30.0
accelerate>=0.20.0
tokenizers>=0.13.0
huggingface_hub>=0.16.0

# Networking and communication
websockets>=11.0
httpx>=0.24.0
pydantic>=2.0.0
toml>=0.10.2

# System monitoring
psutil>=5.9.0

# GPU monitoring (optional, for GPU acceleration)
nvidia-ml-py3>=7.352.0

# OpenVINO NPU support
openvino>=2024.0.0,<2024.5.0
openvino-dev>=2024.0.0,<2024.5.0
optimum[openvino]>=1.18.0,<1.22.0

# Testing
pytest>=7.0.0

# Utilities (with version constraints to resolve conflicts)
tqdm>=4.65.0
numpy>=1.22.4,<2.0.0  # SciPy compatibility (>=1.22.4) + OpenVINO compatibility (<2.0.0)
networkx>=3.0,<=3.1.0  # Constrain for openvino-dev compatibility
pillow>=7.1.0,<11.0.0  # Constrain for potential visualization needs